# SPIKE-004: Authenticated HPKE interoperability

Status: cryptographic core and Keystore-wrapped persistence validated on API 29 and API 36

## Implementation

`core-crypto/AuthenticatedHpke.kt` wraps Bouncy Castle 1.85 RFC 9180 HPKE:

```text
Auth mode
DHKEM(P-256, HKDF-SHA256)
HKDF-SHA256
AES-128-GCM
info = "SyncNotifications-E2EE-v1"
```

`core-crypto/ReplayGuard.kt` retains the in-memory policy model. `core-crypto/AndroidReplayLedger.kt` implements the production persistence boundary with an atomic SQLite transaction keyed by the 32-byte sender key ID and 16-byte message ID. It purges expired entries, rejects duplicates after process/store recreation, and fails closed instead of evicting live entries when capacity is exhausted.

`core-crypto/AndroidHpkeIdentityStore.kt` encrypts the serialized HPKE private scalar with a non-exportable Android Keystore AES-256-GCM key and fails closed on partial or undecryptable state.

`core-protocol/RoutingHeaderV1.kt` implements the fixed 160-byte big-endian Routing Header v1 codec. The exact encoded bytes are HPKE AAD; business message type and notification/action fields remain encrypted.

`core-protocol/EncryptedEnvelopeV1.kt` implements the bounded binary frame. `core-crypto/AuthenticatedEnvelopeReceiver.kt` validates recipient identity and expiry, authenticates HPKE with the original header bytes, and returns plaintext only after the replay tuple is atomically accepted.

`core-protocol/EncryptedPayloadV1.kt` strictly validates canonical protobuf `action.invoke` and `action.result` payloads. `core-crypto/AndroidOperationLedger.kt` persists 30-day sender/idempotency tuples plus completed canonical result bytes. `AuthenticatedActionReceiver.kt` commits replay and operation records before exposing an action, recovers completed results without re-execution, and returns `OUTCOME_UNKNOWN` for reserved operations whose result was lost.

`core-protocol/TrustedDevicePairingV1.kt` implements the server-independent canonical offer/approval records and `sntrust1:` QR representation used by the approval workflow. It validates the complete P-256 point on-curve, non-zero identifiers/nonces, bounded timestamps, exact offer hash, distinct device/key pairs, canonical base64url, and the transcript-derived 60-bit safety code. Decoding or scanning does not write `AndroidTrustedPeerStore`; explicit confirmation remains a separate required step.

`core-crypto/AndroidTrustPairingSessionStore.kt` persists exactly one active offerer or approver transcript in SQLite and refuses silent replacement. `TrustPairingCoordinator.kt` binds every session to the current local workspace/device/public key, restores it after process reconstruction, rejects expired/cross-workspace/self-pairing input, requires the caller to explicitly confirm the exact active safety code, and only then writes the immutable peer pin. It removes the exact session after pinning; if removal is interrupted, retry converges through `ALREADY_PINNED` without key replacement.

`core-notification/AuthenticatedNotificationActionHandler.kt` resolves a random per-revision 16-byte action ID against the process-local `PendingIntent`/`RemoteInput` table. No executable Android capability is serialized.

`AuthenticatedActionResultAckReceiver.kt` verifies an authenticated `action_result_ack` against the completed operation's exact canonical result SHA-256 and the recipient-bound result outbox entry before deletion. `AndroidActionResultOutbox` atomically replaces the live result with a bounded sender/device/digest tombstone so valid duplicate ACKs remain idempotent across process recreation. The production dispatcher performs one pin lookup, one Auth HPKE open, one replay commit, and one canonical decode before routing either `action.invoke` or `action_result_ack`; unsupported authenticated payloads fail closed.

## Evidence

- Android opens the deterministic Chrome-produced authenticated fixture.
- Chrome opens an Android-produced authenticated fixture.
- Both derive identical fixed P-256 key material.
- Sender-key substitution is rejected.
- JVM unit tests pass.
- Instrumented seal/open and sender-substitution test passes on Pixel 10 Pro / Android 16 (API 36).
- Keystore-wrapped identity survives store recreation and remains usable on API 36.
- Persistent replay tuples remain rejected after ledger recreation; expired entries and capacity exhaustion follow fail-closed policy on API 36.
- GitHub Actions Android 10 / API 29 emulator runs HPKE, Keystore persistence, and replay-ledger instrumented tests.
- Kotlin encodes and decodes the same Routing Header v1 bytes as Go and TypeScript and rejects malformed magic, suite, flags, IDs, sequence, and timestamps.
- Kotlin matches the Encrypted Envelope v1 vector, opens its Chrome-generated Auth HPKE ciphertext, and rejects truncation, trailing bytes, bad magic, invalid points, and invalid ciphertext lengths.
- Instrumented receiver tests prove tampered HPKE ciphertext does not consume replay state, a valid frame is accepted once, and its repeat is rejected.
- Kotlin matches the canonical encrypted payload bytes and rejects unknown, duplicate, non-canonical, oversized, and semantically invalid action/result fields.
- Kotlin matches Go's fixed Trusted Device Pairing v1 offer, approval, QR, and `4AFH-Q91K-PGVG` safety-code vector. Exact transcript mutation, wrong offer hash, invalid P-256 point, expired record, padded QR, and whitespace fail closed; no test path creates an approved-peer pin by scanning alone.
- Pairing coordinator instrumentation reconstructs both offerer and approver stores, verifies identical safety codes, proves a wrong confirmation leaves both the pin absent and the durable session present, then confirms each side independently and verifies reciprocal immutable pins plus exact session removal. Cross-workspace and expired offers create no approver session.
- The Compose integration compiles, lints, and packages without adding camera/network permissions or logging pairing content. GitHub Actions run `31555248603` passes protocol verification, unit tests, lint, fixed-signature builds, and API 29 instrumentation for clarified-flow commit `1439e00`.
- On 2026-08-12, the user completed the blocking synthetic approval workflow against Chrome in one freshly created workspace. Android registered through the dedicated loopback relay and reached `ONLINE`, imported Chrome's offer, generated the distinct approval response, displayed the same complete safety code as Chrome, and was explicitly approved locally before Chrome. Chrome remained independently unapproved at that point, proving that Android confirmation did not approve the peer remotely. This is user-observed UI evidence, separate from the reciprocal-pin instrumentation; no real notification or reply content was used.
- On 2026-08-13, the user completed the blocking synthetic relay action/result loop in that approved workspace. The newly installed app-owned test target was exported without notification content or executable capability, Chrome durably queued and sent the encrypted invoke through the authenticated relay, and the user observed Android report `Debug receiver: Regular PendingIntent action received`. Chrome subsequently reconciled the authenticated result as status `1` (`ACTION_RESULT_STATUS_SUCCEEDED`). The relay remained ready and Android remained transport-authenticated. This user-observed side-effect evidence is distinct from the automated execute-once/outbox tests; no real notification, reply, or third-party `PendingIntent` was used.
- Later on 2026-08-13, the user completed the blocking exact-duplicate relay check with the redacted durable counters. A fresh synthetic operation completed with status `1`; Chrome reported two locally accepted invoke deliveries while Android's persisted synthetic side-effect count increased exactly once, from `2` to `3`. After the user explicitly selected `Resend exact completed action`, Chrome's accepted invoke-delivery count increased from `2` to `3` and authenticated result-delivery count increased from `10` to `11`, while Android's side-effect count remained `3`. Thus the duplicate used a fresh envelope delivery and reached Android, but the existing business idempotency key recovered the cached canonical result without re-executing the app-owned `PendingIntent`. Multiple result deliveries are expected until end-to-end ACK exists and do not represent additional side effects.
- The same session then completed the blocking Chrome MV3 Worker restart relay check. `chrome://serviceworker-internals` showed the extension Worker already stopped; the user selected `Start`, the authenticated transport recovered, and the still-open Options page resent the same durable completed operation. Chrome's accepted invoke-delivery count increased from `3` to `4` and authenticated result-delivery count from `11` to `20`, while Android's persisted side-effect count remained `3` and result status remained `1`. This connects real Worker reconstruction of transport/identity/pin/exact-operation state to the relay and Android execute-once boundary.
- The blocking Android process restart relay check then force-stopped the app process; the observed PID changed from `12342` to `17515` when Android recreated the authorized notification-listener process. No new notification, target, or operation was created. After transport recovery, resending the same durable completed operation increased Chrome's accepted invoke-delivery count from `4` to `5` and authenticated result-delivery count from `20` to `25`, while terminal status stayed `1` and Android's persisted side-effect count stayed `3`. The cached success therefore survived process death and was returned without requiring the removed app-owned notification or re-executing its `PendingIntent`.
- The blocking interrupted-network durable-queue check then stopped the loopback relay while both clients retained their durable state. An accidental pre-outage operation had already raised the persisted Android side-effect baseline from `3` to `4`; the actual test operation was separately queued while both clients showed offline. Chrome reported it still pending with zero locally accepted attempts, and Android remained at `4`. After the same `manual-same-workspace.db` relay restarted, both client connections returned without Retry/Connect/Queue input; the pending operation automatically completed with authenticated status `1`, two locally accepted invoke deliveries, and five authenticated result deliveries. Android's side-effect count increased exactly once from `4` to `5`. This proves offline persistence and automatic reconnect delivery without treating local send as execution or creating duplicate side effects.
- The blocking Chrome-side pin-removal relay check used extension `0.1.2` and a fresh successful synthetic operation that raised Android's side-effect count from `5` to `6`. After its result retries settled at one accepted invoke delivery and five authenticated result deliveries, the user explicitly removed the sole approved Android pin through the confirmation-gated UI. Exact resend then reported `Exact resend rejected before encryption: the Android recipient is not approved.` Refresh preserved completed status `1`, invoke count `1`, and result count `5`; Android remained at `6`. Thus pin removal stopped encryption and delivery before the relay without deleting transport membership or creating a side effect.
- Reciprocal trust was then restored only through a fresh Offer/Approval transcript, not by direct pin mutation or old payload reuse. The user confirmed both devices displayed the same complete safety code and approved independently; Android's existing identical Chrome pin converged idempotently while Chrome re-pinned the same Android identity. A fresh post-recovery operation completed with authenticated status `1`, one accepted invoke delivery, and one observed result delivery, while Android's persisted side-effect count increased exactly once from `6` to `7`. The complete baseline/duplicate/restart/interruption/pin-removal/re-approval synthetic relay matrix is therefore manually validated.
- On 2026-08-17, extension `0.1.7` and the fixed-signature Android build completed the blocking end-to-end result-ACK relay validation. Before the fresh operation, Android reported `reservations=9, completed=9, due=0, dormant=9, accepted sends=45` and synthetic side-effect count `8`; the nine entries were historical pre-ACK dormant results. Chrome durably reconciled one authenticated success result and locally accepted bounded ACK deliveries, while Android returned exactly to the same `9/9/0/9/45` live-outbox baseline and the side-effect count increased exactly once to `9`, proving the new result was deleted only after the exact authenticated ACK. An explicit resend then raised Chrome invoke/result deliveries from `1/1` to `2/2`; Android again remained at `9/9/0/9/45`, stayed `ONLINE`, and side-effect count stayed `9`. This validates ACK tombstone reactivation, duplicate result reconciliation, duplicate ACK idempotency, and no repeated `PendingIntent` side effect. The browser Worker-wake resend initially exposed a pre-`SNO1` race; extension `0.1.7` fixed it by waiting for authentication, after which the same durable operation resent successfully.
- The next ACK recovery slice recreates the Android replay ledger, operation ledger, and result outbox after exact result persistence but before ACK receipt, then verifies the ACK deletes that result and a second outbox recreation preserves duplicate-ACK idempotency. The production dispatcher test removes the approved Chrome pin before receiving an exact ACK, proves rejection leaves the result live and does not consume replay, then explicitly reapproves the same key. It also routes an authenticated wrong-digest ACK through the full dispatcher and proves the live result remains until the later exact ACK. Pixel 10 Pro / Android 16 passed all 19 `core-crypto` and the production `core-notification` instrumentation tests. A new redacted snapshot field reports ACK tombstone count without exposing sender, operation, digest, or payload; after reinstalling over the validated state, the app reported `reservations=9, completed=9, due=0, dormant=9, acknowledged=2, accepted sends=45`, `ONLINE`, and side-effect count `9`.
- The blocking real interruption matrix then held one exact synthetic ACK in Chrome Worker memory. Duplicate result delivery changed Android from `reservations/completed/acknowledged/accepted-sends = 9/9/2/45` to `10/10/1/50`, while side-effect count remained `9`. With the relay stopped, Android was force-stopped and restarted from PID `31803` to `2868`; offline reconstruction preserved exact `10/10/1/50`. Chrome Worker reload removed only the memory hold and retained the durable ACK intent with zero accepted deliveries. Explicit authenticated recovery delivered the ACK and Android returned exactly to `9/9/2/45`. A subsequent no-input relay test exposed Chrome's stale MV3 socket state; after Server/Chrome added content-free `SNH1`/`SNH2` heartbeat, the final test kept the relay down for more than 70 seconds and both clients automatically reconnected after restoration. The user observed Chrome `0.1.10 auto-recovered Online`; Android independently remained `ONLINE`, `9/9/2/45`, and side-effect count `9`.
- Android opens the Chrome-produced canonical action envelope and parses its notification ID and revision.
- Instrumented action tests prove replay and persistent operation-idempotency records commit before the side-effect callback; a new envelope with the same idempotency key recovers the stored result without executing twice, an invalid payload still consumes its authenticated replay tuple, and an uncertain crash window fails closed as `OUTCOME_UNKNOWN`.
- Android notification instrumented tests invoke a local test `PendingIntent` exactly once through the encrypted handler, then verify duplicate recovery, `STALE_NOTIFICATION_VERSION`, and `ACTION_NOT_FOUND` without further side effects.
- Pixel 10 Pro / Android 16 (API 36) runtime execution passes all 8 `core-crypto` and 1 `core-notification` instrumented tests, including runtime encryption of a cached result envelope. The user explicitly confirmed that the follow-up ordinary-action and free-form `RemoteInput` UI regression showed no problems after replacing action indexes with per-revision opaque action IDs; this UI conclusion is supported by, but distinct from, the instrumented test evidence.
- `ActionReceipt` exposes the exact canonical `action.result` payload persisted by the operation ledger, and `ActionResultEnvelopeSender` encrypts those bytes independently for one Chrome recipient without exposing result semantics in the frame.
- `AndroidTrustedPeerStore` persists only locally approved workspace/device/P-256 pins, validates the canonical curve point and SHA-256 binding on load, treats an identical pin as idempotent, and refuses replacement until explicit removal. No server directory path can populate it.
- `AndroidActionResultOutbox` first reserves recipient/idempotency capacity before local action execution, then durably completes that reservation with the exact canonical result before any network send. A crash after reservation is invisible to the sender until a new envelope recovers `OUTCOME_UNKNOWN`; a full outbox rejects before the side-effect callback. It restores due entries and positive per-recipient sequences after process recreation, records only locally accepted WebSocket send attempts, becomes dormant rather than deleting an unacknowledged result after the bounded short retry budget, and reactivates exact bytes when a duplicate invoke recovers the cached operation result.
- `AndroidActionInvokeDispatcher` is connected to the process transport coordinator. Authenticated binary frames are serialized, checked against the active workspace/device/key route, resolved only through the immutable approved-peer pin, Auth HPKE-opened, replay-checked, operation-reserved, executed once, and completed into the outbox. Text, unapproved, malformed, replayed, or otherwise rejected inbound data closes the socket into `SECURITY_ERROR` rather than entering network reconnect.
- `ActionResultOutboxDrainer` resolves only still-approved recipients, allocates durable per-recipient sequence numbers, Auth HPKE-encrypts exact cached result bytes, and sends them through the authenticated WebSocket with 1/2/4/8-second bounded retries. Local WebSocket acceptance is not treated as Chrome reconciliation: after five attempts the entry remains dormant until expiry or an identical duplicate invoke reactivates it. Removing a peer pin immediately prevents subsequent encryption attempts.
- Pixel 10 Pro / Android 16 passes all 15 `core-crypto` and 1 `core-notification` instrumentation tests for this boundary, including outbox-capacity rejection before execution, crash recovery to durable `OUTCOME_UNKNOWN`, unapproved-sender rejection before replay consumption, execute-once, runtime result encryption/retry, and revocation preventing later encryption. GitHub Actions run `31472390239` independently passes build, lint, fixed-signature verification, and the corresponding API 29 crypto/notification/transport instrumentation for commit `8aaaf6b`.
- `core-transport` matches the Go `SNA1`/`SNO1` vector, rejects insecure non-loopback origins, performs strict bounded code-gated registration without following redirects, wraps the returned 32-byte credential with Android Keystore AES-GCM bound to server/workspace/device/HPKE identity metadata, sends `SNA1`, and exposes WebSocket `onOpen` to application code only after validating the server's first-message `SNO1` authentication confirmation.
- Pixel 10 Pro / Android 16 (API 36) confirms Keystore-wrapped transport credential restoration and silent-replacement refusal in an instrumented runtime test.
- E2EE Identity Key Transition v1 is vendored with spec hash `665e676d3ac0620cdb10d48a0e0c5ecc1d0ffb2cb1997caf724e9e94a33fb323` and vector hash `f87f605480d320b622d3810a250f445b40ba0bc6aec27a2a6f5630f87d29c3d0`. Java Lite schema and Kotlin canonical codec support only schema-v2 transition/ACK/commit bodies, while existing action bodies remain schema v1. Validation covers non-zero fixed-width IDs, full P-256 point membership, `SHA-256(new_public_key) == new_key_id`, distinct old/new keys, bounded non-zero digests, duplicate/unknown fields, deterministic wire bytes, and schema/body mismatch. All three encodings match the Go/Chrome public vector. `AndroidHpkeIdentityStore` now retains current plus at most one pending private identity under the existing non-exportable Keystore wrapping key. The pending slot uses a distinct AAD domain, preparation never overwrites current, repeated reconstruction returns the byte-identical pending key, partial/same-key state fails closed, and neither raw private scalar appears in SharedPreferences. Pixel 10 Pro / Android 16 instrumentation proves pending-key HPKE decryption after store reconstruction and rejects ciphertext slot swapping. A dedicated pending-local-identity receive boundary now authenticates and decrypts only a canonical `identity_key_transition_ack` addressed to the proposed key, then checks the exact peer device, transition ID, old/new key IDs, and transition digest with constant-time byte comparison before consuming the replay tuple. Authenticated schema-v1 business plaintext, another lifecycle body, a wrong peer device, or any wrong transition binding fails closed without exposing plaintext to a business dispatcher. The generic active-identity receiver remains unchanged. The trusted-peer SQLite schema now migrates from v1 to v2 and atomically retains one exact successor per approved peer: canonical transition bytes/digest, old/new key binding, proposed public key, deterministic canonical ACK intent/digest, seven-day expiry, and `PENDING_COMMIT`/`BLOCKED` phase. Exact duplicate transition delivery returns the same ACK bytes after process/store reconstruction; a different successor fails closed. Expiry durably blocks without replacing either pin, and explicit peer removal deletes both the pin and successor state. The authenticated transition receiver persists this state and ACK intent before replay consumption, so replay-capacity failure or a crash cannot lose an already authenticated transition; a fresh-envelope retry is idempotent. Trusted-peer schema v3 adds durable ACK retry scheduling and recipient-key sequence allocation, with v1→v2→v3 migration. `IdentityTransitionAckOutboxDrainer` requires the current sender identity to match the transport identity binding, encrypts the exact stored ACK to the proposed new key, and uses a fresh message ID and durable sequence for every delivery. `send == false` leaves the intent immediately due; local socket acceptance only advances bounded exponential retry metadata and never deletes the ACK. Five accepted attempts make the entry dormant until the exact transition is received again, which reactivates the same ACK bytes. Store/process reconstruction preserves the intent and sequence, while blocked transitions are never due. `AndroidLocalIdentityTransitionStore` now persists one rotating-device session with its immutable peer snapshot and seven-day deadline. The pending-identity receiver authenticates an ACK against that snapshot, then atomically saves the exact canonical ACK, its SHA-256, and the exact peer-specific canonical commit before consuming replay. Replay-capacity failure therefore leaves recoverable commit work; a fresh-envelope retry returns the byte-identical commit after process/store reconstruction. Wrong peer/key/transition/digest and non-canonical ACKs fail closed, while expiry durably blocks without generating a commit or modifying either identity. The local-transition SQLite schema now migrates to v2 with durable commit retry metadata and recipient-key sequences. `IdentityTransitionCommitOutboxDrainer` requires transport to remain bound to the current old identity, requires the local transition to match the exact current/pending slots, authenticates the exact stored commit with the pending identity, and encrypts it only to the snapshot peer's still-approved current key. Every retry uses a fresh message ID and sequence; `send == false` does not advance accepted-attempt state, while five local socket acceptances make the exact commit dormant until an exact ACK reactivates it. The trusted-peer SQLite schema now migrates to v4 with a bounded committed-transition tombstone. Its dedicated commit-only boundary resolves the sender solely from the durable pending successor, accepts no business payload, verifies the exact transition and ACK digests, then atomically promotes the new pin, removes pending successor state, and stores the exact commit tombstone before replay consumption. A replay-capacity failure therefore leaves the promoted pin recoverable; fresh-envelope duplicate commits remain idempotent across process/store reconstruction. Wrong key/digest leaves the old pin unchanged, the retired old key is no longer approved, and an active tombstone blocks another transition until its seven-day bound expires. Recoverable cross-store local identity/transport promotion now uses a separate non-secret `core-storage` SharedPreferences journal rather than treating the identity, transport, and local-transition stores as one transaction. Promotion starts only when every immutable snapshot peer has an exact ACK and durable commit. The journal binds workspace, local device, transition, old key, and new key, then replayably advances exact transport `identityKeyId` rebind, pending Keystore-wrapped identity promotion, and durable local-session completion. Every external step is idempotent for the exact binding and rejects mismatched state. A transport-credential rotation present before journal preparation blocks preparation; if it races immediately after preparation, identity promotion durably defers so the old-bound pending credential can complete `SNO1`, then resumes without stranding either journal. Instrumentation covers ordinary completion and process-style reconstruction after transport alone, or both transport and identity, committed before the journal phase update. The production transport coordinator runs recovery before selecting a credential or opening a socket; partial/corrupt journal state therefore enters the existing fail-closed security path without network use. Commit delivery supports both the pre-promotion pending sender and the exact promoted current sender, retaining canonical commit retries after old-key destruction. Local unit tests, lint, app build, protocol verification, and crypto/storage AndroidTest APK assembly pass. Commit `a0880fc` passed GitHub Actions run `32155751297`, including all 33 `core-crypto` and 4 new `core-storage` instrumentation tests on API 29, fixed-signature Debug/Release APK builds, and certificate verification. Recovery UX remains pending.

The production Android transport coordinator now creates one lifecycle dispatcher and ACK/commit drainer set for each credential-bound connection. Pending local identities remain ACK-only, durable peer successors remain commit-only, and approved current senders are tried as old-key transitions before the existing action/result-ACK business dispatcher. All paths share the same replay ledger. Transition receipt immediately drains durable ACK intents; ACK receipt drains commits and runs cross-store promotion. If promotion completes, the coordinator zeroes every retained old private-key working copy, closes the old connection, and reconstructs handlers from the promoted current identity before sending further work. `SNO1` authentication and a dedicated scheduled future resume both lifecycle outboxes with their bounded retry metadata; local socket rejection does not advance accepted-attempt state. New API 29 instrumentation verifies transition routing and proves authenticated business plaintext reaches the business fallback without creating successor state. Local unit tests, lint, app build, protocol verification, and AndroidTest assembly pass. Rotation initiation and user recovery controls remain absent, so this runtime path is not yet a user-complete transition workflow. Commit `1faa843` passed GitHub Actions run `32160973156`, including all 34 `core-crypto` and 4 `core-storage` instrumentation tests on API 29, fixed-signature Debug/Release APK builds, and certificate verification.
- Local E2EE identity-transition initiation and old-key transition delivery are now connected to the production coordinator. `AndroidIdentityTransitionInitiator` snapshots every approved peer, reuses an orphaned Keystore-wrapped pending identity after process reconstruction, persists one exact canonical transition, and refuses to run during transport-credential rotation. `IdentityTransitionOutboxDrainer` authenticates with the still-current old identity, encrypts only to each snapshot peer's still-approved key, and retries exact plaintext with fresh message IDs and durable recipient sequences. `send == false` does not advance accepted-attempt state; five accepted sends leave the peer dormant until exact recovery or expiry. The coordinator reconstructs its connection after initiation so pending-ACK routing and transition delivery are active, and Compose requires an explicit warning confirmation. Local unit tests, lint, app/APK builds, protocol verification, and crypto/storage AndroidTest APK assembly pass. The new instrumentation tests were not executed on a local physical device, but commit `38a9508` passed GitHub Actions run `32165398093`, including all 35 `core-crypto`, 5 `core-storage`, 1 notification-action, and 1 transport instrumentation tests on API 29 plus fixed-signature Debug/Release APK verification. Reciprocal real-process interruption/expiry recovery and blocked-peer removal UX remain unverified.
- Blocked/removal recovery is now explicit and user-visible. The local transition store lists exact snapshot peers, durably marks expiry as `BLOCKED`, and permits an expired session to enter `RECOVERY_AUTHORIZED` only after the user removes every remaining unacknowledged peer while at least one acknowledged peer remains. Recovery-authorized commit retries use fresh bounded scheduling rather than the already-expired original deadline. Trust removal commits before snapshot exclusion; if the process stops between those stores, the same exact peer removal resumes without deleting a newly approved different key. Removing the final snapshot peer leaves the session blocked and never promotes automatically. Compose displays only local short device/key references, peer phase, original deadline, explicit warning confirmation, and per-peer removal controls. Local unit tests, lint, app build, protocol verification, and crypto/storage AndroidTest APK assembly pass. Commit `94d034c` passed GitHub Actions run `32207342437`, including all 36 `core-crypto`, 6 `core-storage`, 1 notification-action, and 1 transport instrumentation tests on API 29 plus fixed-signature APK verification; real reciprocal process interruption and response-loss validation remains pending.
- Transport Credential Rotation v1 is vendored at canonical hash `6f8fb759ce11ca2b7f8470b830ae99feaf199ec2272318224deb2ae0cd190294`. Current and at most one pending token use distinct Keystore AES-GCM AAD domains while retaining exact server/workspace/device/HPKE identity metadata. SharedPreferences contains only ciphertext, IV, metadata, and `PREPARED`/`ATTEMPTED`; partial state, same-token, and silent pending replacement fail closed. The strict no-redirect client commits attempted before request, reuses exact pending after ambiguous response loss or process reconstruction, and treats HTTP 200 only as submission evidence. The coordinator probes attempted pending first, falls back to current after pre-`SNO1` termination, and atomically promotes/re-wraps only after `AuthenticatedWebSocketFactory` has consumed exact `SNO1`; promotion failure enters `SECURITY_ERROR`. Compose exposes a password-masked synthetic-only rotation-code control. Unit and API 29 instrumentation cover request ordering, exact retry bytes, response bounds, reconstruction, raw-secret absence, fallback candidates, and promotion. GitHub Actions run `32022040912` passed protocol verification, unit tests, lint, fixed-signature Debug/Release builds and certificate verification, plus the full API 29 crypto/notification/transport instrumentation suite for commit `bc88fd2`.
- On 2026-08-18, the blocking physical-device rotation matrix passed against an isolated LAN TLS origin with zero `adb reverse` entries. Android completed a baseline rotation, promoted only after pending received exact `SNO1`, and returned `ONLINE` after process restart. With the relay unavailable, a second rotation durably retained `ATTEMPTED`; process reconstruction reused the byte-identical wrapped pending token, pending authentication was denied before the server transaction existed, current fallback returned `ONLINE`, and retrying the same authorization committed and promoted that exact pending. A TLS test proxy then allowed the server transaction to commit while dropping the HTTP response and blocking new WSS for 30 seconds. The Android process was force-stopped after the commit; on reconstruction it authenticated the durable pending, promoted, removed all pending/phase metadata, and returned `ONLINE` without an HTTP-success response. Restoring an older Keystore-wrapped transport record produced only `OFFLINE`, proving the rotated credential was permanently rejected; restoring the latest record returned `ONLINE`. Across credential versions `1` through `4`, stable hashes proved the HPKE identity and approved Chrome pin were unchanged, and exact metadata comparison proved workspace, device ID, and HPKE identity binding were unchanged. This real-process evidence is separate from the automated request-ordering and fail-closed tests; only synthetic data was used.
- The Compose app now exposes synthetic-only Android code-gated registration. `NotificationMirroringApplication` owns a process-lifetime transport coordinator that restores an existing credential, requires the existing Keystore-wrapped HPKE identity, verifies the full SHA-256 key ID binding, reports `ONLINE` only after `SNO1`, and clears temporary token and decrypted identity byte arrays. Corrupt/partial identity or credential state enters `SECURITY_ERROR` without replacement. Socket termination schedules one jittered exponential retry sequence bounded from 1 to 60 seconds; duplicate terminal callbacks are collapsed by connection generation, authentication resets backoff, explicit requests cancel stale retries, and `ConnectivityManager` availability triggers an immediate attempt. The E2EE action dispatcher, durable result drainer, and durable approval core are connected at their storage boundaries. `AndroidTrustPairingController` now binds that workflow to the existing transport credential and Keystore-wrapped HPKE identity, verifies their SHA-256 key-ID relationship, clears loaded token/private-key working copies, and exposes serialized operations to Compose. The synthetic-only card supports offer creation, controlled `sntrust1:` payload copy/import, process-restored sessions, full safety-code display, a mandatory explicit comparison checkbox, local approval, rejection/cancellation, and fail-closed recovery. Camera QR rendering/scanning is not implemented yet; textual payload exchange is only the controlled integration path for the upcoming synthetic relay test.
- Android Network Security Configuration denies cleartext globally and permits it only for loopback development origins; non-loopback registration remains HTTPS/WSS-only. The non-loopback validation slice adds a `debug-overrides` trust anchor for CAs explicitly installed by the device user. Android applies that anchor only to debuggable builds, enabling a private development PKI on a physical device without weakening the fixed-signature Release build, which continues to require a system-trusted certificate. `:app:lintDebug` passes.
- GitHub Actions run `31453307739` passed protocol verification, unit tests, lint, fixed-signature APK builds, and API 29 crypto/notification/transport instrumentation for commit `300ca32`. The malformed-acknowledgement WebSocket test waits for the server-side terminal callback and tolerates only the subsequent `MockWebServer.shutdown()` upgrade-task race; the fail-closed and application-not-opened assertions remain mandatory.
- Pixel 10 Pro / Android 16 manual transport lifecycle validation used a dedicated loopback test server through `adb reverse` and synthetic registration data only. The user explicitly observed initial registration reaching `ONLINE`, then observed `ONLINE` again after an ADB force-stop and application restart without re-entering a pairing code. After the server was stopped, the user observed `OFFLINE` and the `Retry connection` button; after the server became ready and the button was pressed, the user observed `ONLINE` and the button disappearing. A later validation installed the bounded-reconnect build while preserving the same credential/identity and recovered automatically after relay restoration.
- The subsequent blocking non-loopback matrix used the physical device over Wi-Fi, a dedicated HTTPS/WSS origin and private development CA, a fresh workspace/identity/credential, and no `adb reverse`. The user observed post-`SNO1` `ONLINE`, matched the complete reciprocal E2EE safety code, and approved independently. One app-owned synthetic action reached terminal `SUCCEEDED`, while Android observed exactly one `PendingIntent` side effect and exact ACK convergence to `reservations/completed/due/dormant = 0/0/0/0`, `acknowledged=1`, accepted sends `0`. After more than 112 seconds of relay outage Android was `OFFLINE`; restoring the same database/certificate/origin returned it to `ONLINE` without Retry or process restart and left every business aggregate unchanged. A separate radio transition disabled LAN reachability, confirmed one remaining Chrome connection and Android `OFFLINE`, then restored Wi-Fi: the device obtained its LAN IP after 2.6 seconds and `ConnectivityManager.onAvailable()` restored authenticated WSS 4.2 seconds later. Original Wi-Fi/mobile-data state was restored. These UI/ADB/server observations are recorded separately from automation; no real notification content was transported. Cleanup then cleared the synthetic app state, removed the copied CA file, deleted all local profile/database/code/key material, and removed the temporary CA from Windows; the user independently confirmed removal of the exact development CA from Android's user trust store without clearing unrelated credentials.

Vendored vectors:

```text
protocol/test-vectors/hpke-auth-p256-aes128gcm.json
protocol/test-vectors/routing-header-v1.json
protocol/test-vectors/encrypted-payload-v1.json
protocol/test-vectors/encrypted-envelope-v1.json
protocol/test-vectors/trusted-device-pairing-v1.json
```

The authoritative vector and ADR-002 live in the server repository.

## Safety boundary

The serialized private scalar and deterministic key derivation exist for spike vectors only. Production Android storage wraps generated HPKE private material with a non-exportable Android Keystore AES-GCM key; application backup is disabled. API 29 and API 36 persistence/restore are proven without logging or transport; corruption/recovery UX still requires validation. Real notification content remains blocked.

## Remaining evidence

- corruption/lost-Keystore recovery UX
- Generic relay cursor and 2 Android × 2 Chrome offline convergence remain separate from the completed per-operation ACK recovery matrix
- camera pairing UX, E2EE identity rotation, client-visible revocation, and lost-device behavior

- 2026-08-24 source retirement: the unused bilateral pairing, trusted-peer, identity-transition, promotion-journal, and peer-removal implementations and their dedicated `core-storage` module were deleted after the authority-roster product cutover. The shared authenticated-envelope boundary now contains business-envelope authentication and replay handling only. The identity store rejects retired persisted pending-identity state and requires administrator revocation plus re-enrollment instead of reviving the old transition path. Canonical pairing/transition specs, hashes, vectors, and generated schema fields remain frozen as historical protocol evidence.
